# Gemini API Configuration
# Required for Gemini API judging (production mode)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# MLflow Configuration (Optional)
# Uncomment to enable MLflow experiment tracking
# MLFLOW_TRACKING_URI=http://localhost:5000
# MLFLOW_EXPERIMENT_NAME=criteria-bind

# Verification Settings (Optional)
# Set to 1 to run live API calls during verification (requires valid API key)
# Set to 0 to use mock mode only (default)
VERIFY_LIVE=0

# Development Settings (Optional)
# LOG_LEVEL=INFO
# TORCH_DEVICE=cuda
